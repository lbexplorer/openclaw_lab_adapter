# AGENTS.md

## Codex 硬护栏

本项目是无人车协同 Demo 项目，主线固定为：

```text
大车固定点巡逻
-> 持续人员检测
-> 检测到静态人员
-> 发送协同指令
-> 小车接收指令
-> 小车前往目标位置
-> 小车执行静态人员跟踪
```

最高优先级：

```text
真实接口和运动安全 > Demo 主线 > 效率和最小读取 > 最小改动 > 稳定可演示 > 代码优雅 > 未来扩展
```

## 仓库边界

- 默认只允许修改当前仓库 `openclaw_lab_adapter`。
- 当前开发目录：`D:\Program Files\科研材料\无人车\openclaw_lab_adapter`。
- `..\sailors_onboard-main` 只作为真实无人车接口参考，真实接口以它为准，未经用户明确授权不得修改。
- `..\openclaw` 只作为 OpenClaw skill 组织方式参考，未经用户明确授权不得修改。
- 不得把项目目标扩展为通用机器人平台。

## 读取规则

- 每次任务先读取 `AGENTS.md`。
- 任务不明显、涉及代码行为、接口、agent、skill、测试或跨模块判断时，再读取 `docs/codex/02_TASK_ROUTER.md`。
- 根据任务只读取目标文件和必要上下文；不要默认完整读取 `CODEX_TASK.md` 或 `docs/codex/00-08` 全部专题文档。
- `CODEX_TASK.md` 是项目说明索引，`docs/codex/00-08` 是参考资料，不是日常默认必读链路。

## 任务分级

### 简单任务

适用：文档-only、说明补充、启动命令、快速测试、错别字、格式、日志、注释、已有能力说明。

处理方式：读取入口规则和目标文件后直接修改并说明验证方式。不要因为出现 `skill`、`agent`、`ROS`、`DDS` 等关键词就自动升级。

### 普通复杂任务

适用：本仓库内的代码实现、已有 skill 局部逻辑、agent 局部路由、测试、配置或文档结构调整；不影响真实机器人运动安全，不改变真实 ROS / DDS / 导航 / 跟踪接口，不引入新依赖或新架构。

处理方式：必要时简要说明计划后直接执行，完成后运行合适验证。

### 高风险任务

适用：真实机器人运动安全、真实 ROS / DDS / 导航 / 跟踪接口变更、`cmd_vel` 或导航目标下发、跨仓库修改、新依赖 / 新进程 / 新通信协议、权限或凭据、不可逆操作、外部系统副作用、大范围架构重构，或需求不明确但可能影响安全边界。

处理方式：先明确计划、风险、验证方式和待确认问题，等待用户确认后再修改。Plan mode 已启用时只使用 Plan mode 的计划机制。

## 进度记录

- `docs/log/DEVELOPMENT_LOG.md` 是项目进度事实源。
- 涉及代码行为、真实接口文档、agent、skill、测试、重要配置或重要文档结构的任务，完成后在 `docs/log/DEVELOPMENT_LOG.md` 最新位置追加简洁记录。
- 纯错别字、格式、临时查询、轻量说明补充不强制记录。
- 历史记录文件可以保留，但后续进度优先维护 `docs/log/DEVELOPMENT_LOG.md`。
