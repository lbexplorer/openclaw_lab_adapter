# codex_task.md

## 1. 项目定位

本项目是一个无人车协同 Demo 项目。

目标不是重新开发完整无人车系统，而是在现有无人车真实运行代码基础上，封装适用于 Demo 的 skills，并开发一个 agent，使 agent 能够通过调用大模型和 skills，驱动无人车完成指定演示任务。

本项目必须始终围绕以下 Demo 闭环展开：

```text
大车固定点巡逻
→ 持续人员检测
→ 检测到静态人员
→ 发送协同指令
→ 小车接收指令
→ 小车前往目标位置
→ 小车执行静态人员跟踪
```

任何新增代码、重构、接口封装、agent 设计，都必须服务于该闭环。

---

## 2. 项目核心目标

Codex 的开发目标是：

1. 基于 `sailors_onboard-main` 理解无人车已有真实能力；
2. 参考 `openclaw` 的 skills 组织方式；
3. 在 `openclaw_lab_adapter` 中封装当前 Demo 所需 skills；
4. 开发一个可调用大模型 API 的 agent；
5. 让 agent 能够理解任务目标，并通过调用 skills 推动 Demo 闭环执行；
6. 保证系统行为稳定、简单、可验证、可演示。

当前阶段只关注 Demo 跑通，不追求复杂泛化能力。

---

## 3. 当前 Demo 范围

### 3.1 必须支持的能力

当前阶段只需要支持以下能力：

- 大车执行固定点位巡逻；
- 巡逻过程中持续检测指定区域内是否存在人员；
- 当检测到人员后，触发协同事件；
- 通过现有通信机制向小车发送指令；
- 小车接收指令后执行响应；
- 小车前往目标位置并执行静态人员跟踪；
- agent 能够组织和调用上述 skills 完成任务。

### 3.2 暂不支持的能力

当前阶段不要主动开发以下能力：

- 动态人员跟踪；
- 多目标人员管理；
- 多车复杂调度；
- 自动地图构建；
- 复杂路径规划；
- 强化学习；
- 重新训练视觉模型；
- 替换 DDS 通信；
- 重构无人车底层控制系统；
- OpenClaw 硬件控制适配；
- 与 Demo 主线无关的通用机器人能力。

---

## 4. 代码仓库角色边界

### 4.1 `sailors_onboard-main`

角色：真实无人车能力参考源。

使用原则：

- 只用于阅读、理解、提炼真实接口；
- 以其中现有接口、通信方式、控制逻辑为准；
- 不直接修改该项目；
- 不复制无关代码；
- 不对其进行架构重构。

Codex 需要从该项目中提炼：

- 已有能力；
- 可调用接口；
- 输入参数；
- 输出结果；
- 调用前置条件；
- 调用后系统行为；
- 错误或异常情况。

---

### 4.2 `openclaw`

角色：skills 组织方式参考源。

使用原则：

- 只参考其 skill 设计风格、目录组织、接口抽象方式；
- 不照搬其硬件实现；
- 不将 OpenClaw 的机械臂逻辑强行迁移到无人车项目；
- 不为了匹配 OpenClaw 而改变无人车真实接口。

Codex 需要从该项目中参考：

- skill 的封装粒度；
- skill 的命名方式；
- skill 的输入输出描述；
- skill 的调用方式；
- skill 与 agent 的关系。

---

### 4.3 `openclaw_lab_adapter`

角色：当前唯一允许开发和修改的主项目。

使用原则：

- 新增代码应优先放在该项目中；
- Demo skills 应在该项目中封装；
- agent 应在该项目中实现；
- 所有修改必须服务于 Demo 闭环；
- 所有新增模块应便于后续接入 OpenClaw / Sailors Onboard 风格的 skill 系统。

除非用户明确授权，不允许修改其他项目。

---

## 5. Skills 设计原则

### 5.1 Skill 的本质

本项目中的 skill 是对无人车已有能力的封装。

skill 不是：

- 重新实现无人车控制；
- 重新实现视觉检测；
- 重新实现通信系统；
- 重新实现路径规划；
- 绕过现有系统做一套新系统。

skill 应该做的是：

- 明确能力边界；
- 统一调用入口；
- 明确输入输出；
- 屏蔽底层调用细节；
- 让 agent 可以稳定调用。

---

### 5.2 Skill 封装顺序

在写代码前，Codex 必须先完成以下分析：

1. 该 skill 对应的真实能力是什么；
2. 该能力在 `sailors_onboard-main` 中由哪些文件或接口提供；
3. 该能力的输入是什么；
4. 该能力的输出是什么；
5. 该能力成功和失败的判断标准是什么；
6. 该 skill 是否直接服务于 Demo 闭环；
7. 是否需要新增代码；
8. 新增代码应放在哪个文件中。

未完成上述分析，不得直接编写 skill 代码。

---

### 5.3 当前建议的 Skill 范围

当前 Demo 阶段优先考虑以下 skills：

```text
patrol_fixed_points
start_person_detection
check_person_detected
publish_tracking_command
receive_tracking_command
track_static_person
stop_vehicle
get_demo_status
```

以上名称不是强制最终命名，但能力边界不得偏离 Demo 主线。

---

## 6. Agent 设计原则

### 6.1 Agent 的角色

agent 的作用是组织任务流程，而不是替代无人车底层系统。

agent 应负责：

- 理解当前 Demo 任务；
- 根据任务目标选择合适 skill；
- 按照 Demo 主线调用 skill；
- 处理 skill 返回结果；
- 在必要时停止、重试或报告失败；
- 通过大模型 API 辅助任务规划和调用决策。

agent 不应负责：

- 直接控制电机；
- 直接处理传感器底层数据；
- 直接实现目标检测算法；
- 直接实现 DDS 底层通信；
- 替代原有无人车控制系统；
- 自行扩展复杂任务目标。

---

### 6.2 Agent 执行主线

agent 的默认任务流程必须是：

```text
确认 Demo 配置
→ 启动大车固定点巡逻
→ 启动或确认人员检测持续运行
→ 循环检查人员检测结果
→ 若未检测到人员，继续巡逻和检测
→ 若检测到人员，发布小车跟踪指令
→ 确认小车接收指令
→ 小车执行静态人员跟踪
→ 输出 Demo 状态
```

如果当前代码无法完整实现上述流程，Codex 应先报告缺失能力，而不是自行发明替代流程。

---

## 7. 开发约束

Codex 必须遵守以下约束：

1. 真实接口以 `sailors_onboard-main` 为准；
2. `openclaw` 只作为 skills 设计参考；
3. 不重构原有无人车系统；
4. 不照搬 OpenClaw 的硬件实现；
5. 不扩展与当前 Demo 无关的能力；
6. 不重新训练模型；
7. 不替换 DDS；
8. 不处理动态人员跟踪；
9. 所有新增代码必须服务于 Demo 闭环；
10. skills 是对已有能力的封装，不是重新实现机器人能力；
11. `openclaw_lab_adapter` 是唯一允许修改的主项目；
12. `sailors_onboard-main` 仅作参考，不直接修改；
13. 优先提炼能力边界、输入输出、调用方式，再封装 skill；
14. 生成的代码需便于后续接入 OpenClaw / Sailors Onboard skill 系统；
15. Codex 应根据任务复杂度选择工作流：正常工作流可直接执行小范围修改，复杂任务工作流必须先给出计划并等待用户确认；
16. 未经用户确认，不得直接执行复杂任务、影响真实机器人运动安全的改动、跨模块大范围修改或用户明确要求先确认的改动；
17. 不得为了“看起来更完整”而引入当前 Demo 不需要的模块；
18. 不得将项目目标改写为通用机器人平台；
19. 不得改变 Demo 主线；
20. 不得忽略静态人员测试这一当前阶段边界。

---

## 8. 两类开发工作流

Codex 默认使用正常工作流。只有任务复杂、风险较高、影响范围较大，或用户主动要求先计划时，才使用复杂任务工作流。

### 8.1 正常工作流

适用场景：

- 文档补充、错别字修正、格式调整；
- 已有代码的小范围 bugfix；
- 变量名、日志、注释、配置项等局部微调；
- 针对已有实现的轻量测试修复；
- 用户已经明确指定要改什么，并且影响范围清楚；
- 不影响真实机器人运动安全；
- 不改变 Demo 主线、不引入新能力、不跨多个核心模块。

正常工作流要求：

```text
阅读相关上下文
→ 直接修改 openclaw_lab_adapter
→ 运行或说明验证方式
→ 简要汇报结果
```

正常工作流不需要每次先输出完整开发计划，也不需要等待用户再次确认。

### 8.2 复杂任务工作流

以下情况必须使用复杂任务工作流：

- 新增完整 skill；
- 修改 agent 主流程、状态机或工具调用策略；
- 涉及巡逻、检测、DDS、导航、跟踪等关键接口变更；
- 影响真实机器人运动、安全边界或底层控制链路；
- 跨多个模块或文件的大范围修改；
- 引入新依赖、新进程、新通信协议或新配置体系；
- 可能偏离 Demo 主线；
- 需求不明确，存在多种实现路线；
- 用户明确要求先给方案、先分析、先计划或先确认。

复杂任务工作流必须先输出简要开发计划，并等待用户确认后再修改。开发前计划只需要说明：

```text
本次开发目标：
- 

需要先阅读或确认的关键内容：
- 
计划修改的文件：
- 

计划新增的文件：
- 
预期效果：
- 

可能风险：
- 

需要用户确认的问题：
```

---

## 9. 修改后的输出要求

完成修改后，Codex 应根据任务大小输出结果。

正常工作流可简要输出：

```text
本次完成内容：
-

验证方式：
-

下一步建议：
-
```

复杂任务工作流完成后，Codex 必须输出：

```text
本次完成内容：
- 

新增功能：
- 

修复问题：
- 

运行或测试方式：
- 

当前未完成事项：
- 

下一步建议：
- 
```

---

## 10. 判断是否偏离主线的标准

当 Codex 准备新增功能时，必须自检以下问题：

1. 这个功能是否直接服务于：
   - 巡逻；
   - 人员检测；
   - 检测触发；
   - 指令发送；
   - 小车接收；
   - 静态人员跟踪；
   - agent 调用 skills？
2. 这个功能是否当前 Demo 必须？
3. 是否能用更简单方式完成？
4. 是否会修改不该修改的项目？
5. 是否引入了新的复杂依赖？
6. 是否改变了原有无人车系统？
7. 是否把静态 Demo 扩展成动态复杂系统？

如果答案不明确，必须先询问用户。

---

## 11. 当前阶段验收标准

当前阶段的 Demo 视为完成，当且仅当满足：

1. 大车能够执行固定点巡逻；
2. 人员检测能够持续运行；
3. 检测到静态人员后能够触发事件；
4. 指令能够发送给小车；
5. 小车能够接收指令；
6. 小车能够执行静态人员跟踪；
7. agent 能够通过 skills 组织上述流程；
8. 整个流程可以重复演示；
9. 不需要人工手动触发中间环节；
10. 没有引入与 Demo 无关的复杂能力。

---

## 12. Codex 的默认工作方式

Codex 应始终先阅读项目目标和相关代码，再根据任务复杂度选择工作流。

正常工作流：

```text
阅读项目目标
→ 阅读相关代码
→ 判断属于小范围修改
→ 直接修改 openclaw_lab_adapter
→ 运行或说明测试方式
→ 简要汇报结果
```

复杂任务工作流：

```text
阅读项目目标
→ 阅读相关代码
→ 提炼已有能力
→ 明确 skill 边界
→ 给出修改计划
→ 等待用户确认
→ 修改 openclaw_lab_adapter
→ 运行或说明测试方式
→ 汇报结果
→ 建议下一步
```

不要跳过“分析”阶段。只有复杂任务、高风险任务或用户明确要求时，才必须进入“计划确认”阶段。

---

## 13. 最高优先级原则

如果不同原则之间发生冲突，按照以下优先级处理：

```text
Demo 主线 > 真实接口 > 最小改动 > 稳定可演示 > 代码优雅 > 未来扩展
```

---

## 14. 一句话任务指令

Codex 的任务是：

> 基于现有无人车真实运行能力，参考 OpenClaw 的 skill 组织方式，在 `openclaw_lab_adapter` 中封装 Demo 所需 skills，并开发 agent，让系统稳定跑通“大车巡逻 → 发现静态人员 → 通知小车 → 小车跟踪”的演示闭环。
